{
  "name": "tailwind-sandbox",
  "scripts": {
    "start": "parcel index.html",
    "build": "rm dist -rf && rm .cache -rf && parcel build index.html"
  },
  "dependencies": {
    "tailwindcss": "^1.1.4"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^1.3.0",
    "parcel-bundler": "^1.12.4"
  }
}
